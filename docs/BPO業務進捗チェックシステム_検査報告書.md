# BPO業務進捗チェックシステム 検査報告書

**システム名：** BPO業務進捗チェックシステム
**バージョン：** Ver.1.0.0
**検査実施日：** 2026年03月02日
**システムURL：** https://v0-timezone-task-app.vercel.app/
**作業者アカウント：** user/user
**管理者アカウント：** admin/admin

**【結果記号】** ○：合格　×：不合格　△：条件付き合格　-：未実施

---

## 試験項目目次

| No. | シート名 | 機能ID | 試験項目数 |
|---|---|---|---|
| 1 | (1)作業者ログイン画面 | LG-001/003/004 | 8 |
| 2 | (2)管理者ログイン画面 | LG-002/003/004/005 | 10 |
| 3 | (3)地域選択画面 | TK-001 | 8 |
| 4 | (4)タスク一覧画面 | TK-002~012 | 15 |
| 5 | (5)業務日報入力画面 | TK-013/014 | 11 |
| 6 | (6)管理者ダッシュボード | AD-001~006 | 15 |
| | | **合計** | **67** |

---

## (1) 作業者ログイン画面（LG-001/003/004）

試験件数: 正常系 5件 / 異常系 3件 / 合計 8件

| No. | 大分類 | 中分類 | 小分類 | 評価確認項目 | 期待結果 | 結果 | 報告内容 |
|---|---|---|---|---|---|---|---|
| 01 | 認証機能 | ログイン画面 | 正常系 | ログイン画面の初期表示を確認する | ID・パスワード入力欄とログインボタンが表示されること | ○ | `login-form.tsx`にて`loginId`テキスト入力欄、`password`パスワード入力欄、「ログイン」submitボタンが実装済み。ラベル「ログインID」「パスワード」も表示される。role="user"時は「作業者ログイン」と表示される。 |
| 02 | 認証機能 | 作業者ログイン | 正常系 | ID:user / PW:user で正しくログインできる | ログイン成功後、地域選択画面へ遷移すること | ○ | `CREDENTIALS.user`に`{id:"user", password:"user"}`が定義済み。認証成功時に`sessionStorage.setItem("auth-user","true")`を実行し、`onSuccess`コールバックにより`page.tsx`の`authenticated`がtrueとなり`<TaskChecker />`（地域選択画面）が表示される。 |
| 03 | 認証機能 | 作業者ログイン | 正常系 | ログイン後の画面タイトルが正しく表示される | 「地域を選択してください」が表示されること | ○ | `task-checker.tsx`の`RegionSelector`コンポーネント内で「地域を選択してください（担当者を選択後、地域をタップ）」というテキストが表示される。 |
| 04 | 認証機能 | セッション管理 | 正常系 | ページ更新後もログイン状態が維持される | sessionStorageにより同一タブ内でセッションが維持されること | ○ | `page.tsx`のuseEffectで`sessionStorage.getItem("auth-user")`を確認し、"true"であればログイン画面をスキップして直接`<TaskChecker />`を表示する。sessionStorageは同一タブ内で永続するため、ページ更新後も維持される。 |
| 05 | 認証機能 | ログアウト | 正常系 | ログアウトボタンを押してログイン画面へ遷移する | ログアウト後にログイン画面へ遷移し、セッションが削除されること | × | **ログアウトボタンが未実装。** 現在の実装ではヘッダーやフッターにログアウト機能が存在しない。sessionStorageを手動でクリアするか、タブを閉じる以外にログアウトする手段がない。ログアウトボタンの追加が必要。 |
| 06 | 認証機能 | 作業者ログイン | 異常系 | 誤ったIDでログインを試みる | エラーメッセージが表示され、ログインが拒否されること | ○ | `handleSubmit`関数内で`loginId === cred.id && password === cred.password`の条件判定を行い、不一致時に`setError("ログインIDまたはパスワードが正しくありません")`を実行。エラーメッセージがフォーム下部に赤色（text-destructive）で表示される。 |
| 07 | 認証機能 | 作業者ログイン | 異常系 | 誤ったパスワードでログインを試みる | エラーメッセージが表示され、ログインが拒否されること | ○ | No.06と同様のロジックにより、パスワードのみ誤った場合もエラーメッセージが表示され、ログインが拒否される。 |
| 08 | 認証機能 | 作業者ログイン | 異常系 | ID・パスワードを空欄でログインを試みる | エラーメッセージが表示され、ログインが拒否されること | ○ | 空文字列は`cred.id`("user")と一致しないため、同じエラーメッセージ「ログインIDまたはパスワードが正しくありません」が表示される。ただし、HTMLの`required`属性は設定されていないため、フォーム送信自体は実行されるが、バリデーションロジックにより拒否される。 |

---

## (2) 管理者ログイン画面（LG-002/003/004/005）

試験件数: 正常系 7件 / 異常系 3件 / 合計 10件

| No. | 大分類 | 中分類 | 小分類 | 評価確認項目 | 期待結果 | 結果 | 報告内容 |
|---|---|---|---|---|---|---|---|
| 01 | 認証機能 | 管理者ログイン | 正常系 | 管理者ログイン画面の初期表示を確認する | 管理者ログイン画面が表示され、ID・パスワード入力欄とログインボタンが確認できること | ○ | `LoginForm`コンポーネントがrole="admin"で呼び出され、「管理者ログイン」ラベルとともにID・パスワード入力欄、「ログイン」ボタンが表示される。 |
| 02 | 認証機能 | 管理者ログイン | 正常系 | ID:admin / PW:admin で正しくログインできる | ログイン成功後、管理者ダッシュボード画面へ遷移すること | ○ | `CREDENTIALS.admin`に`{id:"admin", password:"admin"}`が定義済み。認証成功時に`sessionStorage.setItem("auth-admin","true")`を実行し、`admin/page.tsx`の`authenticated`がtrueとなり`<AdminDashboard />`が表示される。 |
| 03 | 認証機能 | 管理者ログイン | 正常系 | 管理者専用UIが表示される | 作業者画面とは異なる管理者専用の画面が表示されること | ○ | 管理者画面は`AdminDashboard`コンポーネントで構成され、タイトル「地域別 進捗管理」、日付選択プルダウン、全体進捗カード、CSVダウンロード、地域別進捗グリッド、ドキュメントDL、業務日報閲覧など、作業者画面とは完全に異なるUIが表示される。 |
| 04 | 認証機能 | セッション管理 | 正常系 | 管理者ページ更新後もログイン状態が維持される | sessionStorageにより同一タブ内でセッションが維持されること | ○ | `admin/page.tsx`のuseEffectで`sessionStorage.getItem("auth-admin")`を確認し、"true"であればログイン画面をスキップする。sessionStorageは同一タブ内で永続。 |
| 05 | 認証機能 | ログアウト | 正常系 | 管理者ログアウトボタンを押してログイン画面へ遷移する | ログアウト後にログイン画面へ遷移し、セッションが削除されること | × | **ログアウトボタンが未実装。** 作業者画面と同様、管理者画面にもログアウトボタンが存在しない。タブを閉じる以外にログアウトする手段がないため、ログアウトボタンの追加が必要。 |
| 06 | 認証機能 | 権限管理 | 正常系 | 作業者アカウントで管理者画面URLへ直接アクセスする | アクセスが拒否され、ログイン画面またはリダイレクトされること | △ | 作業者としてログイン済みでも、`/admin`へアクセスすると`sessionStorage.getItem("auth-admin")`が確認される。作業者の`auth-user`とは独立して管理されているため、管理者未認証の場合は管理者ログイン画面が表示される。ただし、admin/adminの固定認証情報を入力すればログイン可能であり、**ロールベースのアクセス制御（作業者アカウントで管理者画面を完全にブロックする仕組み）は未実装。** |
| 07 | 認証機能 | 権限管理 | 正常系 | 管理者アカウントで作業者画面URLへ直接アクセスする | 適切な画面へリダイレクトまたはアクセスできないこと | △ | No.06と同様、`auth-admin`と`auth-user`は独立管理のため、管理者ログイン済みでも`/`にアクセスすると作業者ログイン画面が表示される。user/userを入力すれば作業者画面にもアクセス可能。**相互排他的なアクセス制御は未実装。** |
| 08 | 認証機能 | 管理者ログイン | 異常系 | 誤ったIDでログインを試みる | エラーメッセージが表示され、ログインが拒否されること | ○ | 作業者ログイン画面と同一の`LoginForm`コンポーネントを使用。`loginId === "admin"`の条件不一致によりエラーメッセージが表示される。 |
| 09 | 認証機能 | 管理者ログイン | 異常系 | 誤ったパスワードでログインを試みる | エラーメッセージが表示され、ログインが拒否されること | ○ | No.08と同様のロジックにより、パスワード不一致時にエラーメッセージが表示される。 |
| 10 | 認証機能 | 権限管理 | 異常系 | 別タブで同一アカウントにログインする | sessionStorageのため別タブでは独立してセッションが管理されること | ○ | sessionStorageはブラウザのタブごとに独立したストレージ領域を持つ。タブAでログインしてもタブBには影響しない。各タブで独立してログイン操作が必要。 |

---

## (3) 地域選択画面（TK-001）

試験件数: 正常系 8件 / 異常系 0件 / 合計 8件

| No. | 大分類 | 中分類 | 小分類 | 評価確認項目 | 期待結果 | 結果 | 報告内容 |
|---|---|---|---|---|---|---|---|
| 01 | タスク入力 | 地域選択 | 正常系 | 地域選択画面の初期表示を確認する | 7地域（関東・関西・九州・東海・北海道・東北・沖縄）のボタンが表示されること | ○ | `RegionSelector`コンポーネント内で`regionList`を`map`して7地域のカードを表示。`regionList`は`task-data.ts`にて関東・関西・九州・東海・北海道・東北・沖縄の7件が定義されている。各カードには担当者選択プルダウン、地域名、進捗バーが含まれる。 |
| 02 | タスク入力 | 地域選択 | 正常系 | 「関東」を選択してタスク画面へ遷移する | 関東のタスク一覧画面が表示されること | ○ | 「関東」カードクリック時に`onSelectRegion("kanto")`が実行され、`selectedRegion`が"kanto"に設定される。画面が地域選択からタスク一覧に切り替わり、「関東のタスク」タイトルが表示される。 |
| 03 | タスク入力 | 地域選択 | 正常系 | 「関西」を選択してタスク画面へ遷移する | 関西のタスク一覧画面が表示されること | ○ | `onSelectRegion("kansai")`により「関西のタスク」一覧が表示される。動作はNo.02と同一ロジック。 |
| 04 | タスク入力 | 地域選択 | 正常系 | 「九州」を選択してタスク画面へ遷移する | 九州のタスク一覧画面が表示されること | ○ | `onSelectRegion("kyushu")`により「九州のタスク」一覧が表示される。動作はNo.02と同一ロジック。 |
| 05 | タスク入力 | 地域選択 | 正常系 | 「東海」を選択してタスク画面へ遷移する | 東海のタスク一覧画面が表示されること | ○ | `onSelectRegion("tokai")`により「東海のタスク」一覧が表示される。動作はNo.02と同一ロジック。 |
| 06 | タスク入力 | 地域選択 | 正常系 | 「北海道」を選択してタスク画面へ遷移する | 北海道のタスク一覧画面が表示されること | ○ | `onSelectRegion("hokkaido")`により「北海道のタスク」一覧が表示される。動作はNo.02と同一ロジック。 |
| 07 | タスク入力 | 地域選択 | 正常系 | 「東北」を選択してタスク画面へ遷移する | 東北のタスク一覧画面が表示されること | ○ | `onSelectRegion("tohoku")`により「東北のタスク」一覧が表示される。動作はNo.02と同一ロジック。 |
| 08 | タスク入力 | 地域選択 | 正常系 | 「沖縄」を選択してタスク画面へ遷移する | 沖縄のタスク一覧画面が表示されること | ○ | `onSelectRegion("okinawa")`により「沖縄のタスク」一覧が表示される。動作はNo.02と同一ロジック。 |

---

## (4) タスク一覧画面（TK-002~012）

試験件数: 正常系 13件 / 異常系 2件 / 合計 15件

| No. | 大分類 | 中分類 | 小分類 | 評価確認項目 | 期待結果 | 結果 | 報告内容 |
|---|---|---|---|---|---|---|---|
| 01 | タスク入力 | タスク一覧 | 正常系 | タスク一覧画面の初期表示を確認する | 選択した地域名と当日の日付が表示され、デフォルトタスク（TS-001~TS-025）が一覧表示されること | △ | 地域名（例：「関東のタスク」）は表示される。デフォルトタスクは`defaultTimeSlots`に定義された全25件が表示される。**ただし、当日の日付は画面上に明示的に表示されていない。** 日付表示の追加を推奨。 |
| 02 | タスク入力 | 理想時刻表示 | 正常系 | 各タスクの理想完了時刻が表示される | TS-001~TS-025の理想時刻（7:30~16:30）が正しく表示されること | ○ | テーブルの「理想時間」列にて、`slot.time`の値（7:30, 8:00, 9:30, 11:40, 12:00, 13:00, 13:00~, 15:00, 16:30）がrowSpanで各時間帯の先頭行に表示される。 |
| 03 | タスク入力 | タスク完了 | 正常系 | タスクの完了チェックボックスをクリックする | チェックが入り、完了時刻が自動記録されること | ○ | `TaskCheckbox`クリック時に`toggleTask`が実行され、`completedTasks`配列に`{taskId, regionId, completedAt: "HH:MM"}`が追加される。チェックボックスがprimary色に変化し、「完了時刻」列に現在時刻が表示される。 |
| 04 | タスク入力 | タスク完了 | 正常系 | 完了チェックを再度クリックして取り消す | チェックが外れ、完了時刻がリセットされること | ○ | `toggleTask`で既存のcompletedTaskを検出した場合、`filter`で該当レコードを除去する。チェックボックスが未完了状態に戻り、完了時刻は"-"に戻る。 |
| 05 | タスク入力 | 完了時刻記録 | 正常系 | タスク完了時に現在時刻が記録される | 完了時刻として現在の日本時間が記録・表示されること | ○ | `new Date()`で現在時刻を取得し、`HH:MM`形式の文字列に変換して`completedAt`に保存。`getCompletedTime`で取得し「完了時刻」列に表示。ブラウザのタイムゾーン設定に依存するため、日本のブラウザでは日本時間となる。 |
| 06 | タスク入力 | 進捗バー | 正常系 | タスクを完了するたびに進捗バーが更新される | 完了タスク数/全タスク数の割合でプログレスバーが表示されること | ○ | `getRegionProgress`関数で`completed/total`を計算し、地域選択画面のプログレスバー（`width: percentage%`）に反映。タスクの完了/取り消し時にstate更新により自動再描画される。 |
| 07 | タスク入力 | カスタムタスク追加 | 正常系 | カスタムタスク名を入力して追加する | 入力したタスク名でカスタムタスクが一覧に追加されること | ○ | 「タスク追加」ボタンでフォームを表示。時間帯とタスク名を入力して「追加」ボタンを押すと`addCustomTask`が実行され、`customTasks`配列に新タスクが追加される。`getMergedTimeSlots`により時刻順にソートされた状態でテーブルに表示される。 |
| 08 | タスク入力 | カスタムタスク削除 | 正常系 | カスタムタスクを削除する | カスタムタスクが一覧から削除されること | ○ | カスタムタスクの横にXボタンが表示され（`isCustom`判定）、クリック時に`removeCustomTask`で`customTasks`配列から除去、関連する`completedTasks`も同時に削除される。 |
| 09 | タスク入力 | 自動保存 | 正常系 | タスクの進捗データがlocalStorageに保存される | ページ更新後も同日のタスク進捗が保持されること | ○ | useEffectにより`completedTasks`, `report`, `regionAssignments`, `customTasks`の変更を監視し、変更の度に`saveDayData`で`localStorage`に保存（キー：`task-checker-v3-YYYY-MM-DD`）。初回ロード時に`loadDayData`で復元。 |
| 10 | タスク入力 | データ保持期間 | 正常系 | 2ヶ月以内のデータが保持される | 2ヶ月以内のデータはlocalStorageに保持されること | ○ | `cleanupOldData`関数が初回ロード時に実行され、`task-checker-v3-`プレフィックスのキーのうち2ヶ月前の日付(`twoMonthsAgo.setMonth(getMonth()-2)`)より古いもののみを削除。2ヶ月以内のデータは保持される。 |
| 11 | タスク入力 | 画面遷移 | 正常系 | 地域選択画面に戻るボタンを押す | 地域選択画面へ戻ること | ○ | 「← 地域選択に戻る」ボタンのクリックで`setSelectedRegion(null)`が実行され、`selectedRegion === null`の条件により`RegionSelector`コンポーネントが表示される。 |
| 12 | タスク入力 | 遅延検知 | 正常系 | 理想時刻+30分を過ぎてもタスクが未完了の場合 | タスク行が黄色でハイライトされ、遅延アラートが表示されること | △ | `isLate`関数で理想時間+30分超の判定を実装。**ただし遅延検知はタスク完了時に判定される仕様であり、未完了のまま時間が経過した場合のリアルタイムアラートは未実装。** 完了時に30分超過していた場合、完了時刻セルが`bg-yellow-100`、チェックボックスが`bg-yellow-300`で黄色表示される。 |
| 13 | タスク入力 | カスタムタスク追加 | 異常系 | タスク名を空欄で追加を試みる | エラーまたは追加が行われないこと | ○ | `addCustomTask`関数の先頭で`if (!newTaskTime \|\| !newTaskLabel) return;`により空値チェックを実施。さらに「追加」ボタンに`disabled={!newTaskTime \|\| !newTaskLabel}`が設定されており、空欄時はボタンが無効化される。 |
| 14 | タスク入力 | カスタムタスク削除 | 異常系 | デフォルトタスクの削除を試みる | デフォルトタスクは削除できないこと | ○ | Xボタンは`isCustom(task.id)`がtrueの場合のみ表示される。`isCustom`は`customTasks.some(ct => ct.task.id === taskId)`で判定するため、デフォルトタスクにはXボタンが表示されず、削除操作自体が不可能。 |
| 15 | タスク入力 | データ自動削除 | 正常系 | 2ヶ月以上前のデータが自動削除される | 2ヶ月以上前のデータは自動的に削除されること | ○ | `cleanupOldData`でlocalStorageの全キーを走査し、`task-checker-v3-`プレフィックスのキーのうちcutoff日付より古いものを`localStorage.removeItem`で削除する。 |

---

## (5) 業務日報入力画面（TK-013/014）

試験件数: 正常系 10件 / 異常系 1件 / 合計 11件

| No. | 大分類 | 中分類 | 小分類 | 評価確認項目 | 期待結果 | 結果 | 報告内容 |
|---|---|---|---|---|---|---|---|
| 01 | 業務日報 | 日報画面 | 正常系 | 業務日報入力画面が表示される | 作業時間・特記事項などの入力欄が表示されること | ○ | タスク一覧画面の下部に「D.I.GATE(株) 業務日報」セクションが表示される。「本日の新規事案」「業務上の共有事項」の2セクションがあり、クリックで展開すると入力欄が表示される。 |
| 02 | 業務日報 | 日報入力 | 正常系 | 業務日報に作業内容を入力して保存する | 入力内容がlocalStorageに保存されること | ○ | テキスト入力後「追加」ボタンまたはEnterキーで`addItem`が実行され、`report.newItems`または`report.sharedItems`にReportItemが追加される。自動保存のuseEffectにより即座にlocalStorageに保存される。 |
| 03 | 業務日報 | 日報入力 | 正常系 | 保存した日報を再度開くと内容が表示される | 保存した日報内容が正しく表示されること | ○ | 初回ロード時に`loadDayData`で`report`データを復元し、`setReport`で状態を設定。日報セクションを展開すると保存済みの箇条書きアイテムが表示される。 |
| 04 | 業務日報 | 自動記録 | 正常系 | 地域情報が日報に自動記録される | 選択した地域名が日報に自動で記録されること | △ | **日報データに地域情報を自動記録する機能は未実装。** 日報は地域に依存せず共通データとして保存される。CSV出力時には地域ごとのタスクデータと日報データが別セクションで出力されるが、日報自体に地域情報は含まれない。 |
| 05 | 業務日報 | 自動記録 | 正常系 | 完了タスク数・未完了タスク数が日報に反映される | タスク一覧の完了状況が日報に反映されること | △ | **日報セクションにタスク完了数/未完了数のサマリ表示は未実装。** タスクの進捗状況は地域選択画面のプログレスバーと管理画面のダッシュボードで確認できるが、日報内への自動反映は行われていない。 |
| 06 | 業務日報 | 日報確認 | 正常系 | 過去の日報を参照できる | 保存済みの過去日報が確認できること | △ | **作業者画面では過去の日報を参照する機能は未実装。** 管理者画面の日付選択機能で過去の日報データを閲覧可能だが、作業者画面からは当日分のみ入力・閲覧可能。 |
| 07 | 業務日報 | 日報確認 | 正常系 | 日付を指定して日報を検索できる | 指定日付の日報が表示されること | △ | **作業者画面では日付指定機能は未実装。** 管理者画面（AdminDashboard）では日付プルダウンにより過去の日報データを参照可能。 |
| 08 | 業務日報 | 日報データ保存 | 正常系 | 日報データがlocalStorageに保存される | ページ更新後も日報データが保持されること | ○ | `DaySaveData`オブジェクト内の`report`プロパティとしてlocalStorageに自動保存される。ページ更新後も`loadDayData`で復元。 |
| 09 | 業務日報 | データ保持期間 | 正常系 | 2ヶ月以内の日報データが保持される | 2ヶ月以内の日報データはlocalStorageに保持されること | ○ | 日報データはタスクデータと同一の`DaySaveData`オブジェクトに含まれるため、`cleanupOldData`の2ヶ月保持ルールが同様に適用される。 |
| 10 | 業務日報 | 日報出力 | 正常系 | 日報を印刷または出力できる | 日報の印刷プレビューまたは出力機能が動作すること | △ | **日報単体の印刷・出力機能は未実装。** 管理者画面の全体CSVダウンロードに日報データ（新規事案・共有事項）が含まれるが、日報単体の印刷プレビュー機能はない。ブラウザの印刷機能（Ctrl+P）で画面ごと印刷は可能。 |
| 11 | 業務日報 | 日報入力 | 異常系 | 必須項目を未入力で保存を試みる | エラーメッセージが表示されること、または入力検証が行われること | ○ | `addItem`関数で`if (!text.trim()) return;`による空文字チェックを実施。また「追加」ボタンに`disabled={!newItemText.trim()}`（sharedも同様）が設定されており、空欄時はボタンが無効化される。 |

---

## (6) 管理者ダッシュボード（AD-001~006）

試験件数: 正常系 12件 / 異常系 3件 / 合計 15件

| No. | 大分類 | 中分類 | 小分類 | 評価確認項目 | 期待結果 | 結果 | 報告内容 |
|---|---|---|---|---|---|---|---|
| 01 | 管理者機能 | ダッシュボード | 正常系 | 管理者ダッシュボードが表示される | 全地域の当日タスク進捗が一覧表示されること | ○ | `AdminDashboard`コンポーネント表示時に`selectedDate`が当日（`getDateKey()`）に初期化され、`loadDayData`で当日データを取得。全体進捗カード（完了数/総数、パーセント、プログレスバー）と地域別進捗グリッドが表示される。 |
| 02 | 管理者機能 | 進捗確認 | 正常系 | 各地域の進捗率が表示される | 7地域それぞれの完了率（%）が表示されること | ○ | `regionList.map`で7地域をループし、各地域の`getCompletedCountForRegion`/`getRegionTaskCount`で完了率を計算。パーセント表示、プログレスバー、「X / Y 完了」テキストが地域カードに表示される。 |
| 03 | 管理者機能 | 進捗確認 | 正常系 | 遅延タスクがある地域が強調表示される | 遅延のある地域が視覚的に区別されること | × | **管理者ダッシュボードでの地域レベルの遅延強調表示は未実装。** 完了率100%の地域は緑色（`text-green-500`, `bg-green-500`）で表示されるが、遅延タスクを含む地域の視覚的区別は行われていない。遅延は作業者画面の個別タスクレベルでのみ黄色表示される。 |
| 04 | 管理者機能 | UI確認 | 正常系 | 管理者画面のUIが作業者画面と区別されている | 管理者専用のUIデザインが適用されていること | ○ | ヘッダー「地域別 進捗管理」、戻るボタン（ChevronLeft）、日付選択、全体進捗カード（BarChart3アイコン）、CSVダウンロードボタン、ドキュメントセクション、業務日報閲覧セクションなど、作業者画面とは完全に異なるUI構成。フッターの「管理画面」タブがprimary色でアクティブ表示。 |
| 05 | 管理者機能 | CSV出力 | 正常系 | タスクデータをCSV形式でダウンロードする | BOM付きUTF-8形式のCSVファイルがダウンロードされること | ○ | `downloadCSV`関数で`new Uint8Array([0xef, 0xbb, 0xbf])`のBOMを追加し、`Blob`で`text/csv;charset=utf-8`のCSVを生成。`<a>`要素のdownload属性で「業務進捗_全体_YYYY-MM-DD.csv」のファイル名でダウンロード。 |
| 06 | 管理者機能 | CSV出力 | 正常系 | CSVファイルをExcelで開いて文字化けしない | BOM付きUTF-8形式でExcelで正しく開けること | ○ | BOM（`0xEF 0xBB 0xBF`）を先頭に付加しているため、Excelで開いた際にUTF-8として正しく認識され、日本語の文字化けが発生しない。 |
| 07 | 管理者機能 | CSV出力 | 正常系 | CSVに全地域のデータが含まれる | 全7地域のタスクデータがCSVに出力されること | ○ | `downloadCSV`で`regionList`を全件ループし、各地域名・担当者・全タスク・完了状態・完了時刻・遅延・備考をCSVに出力。さらに業務日報（新規事案・共有事項）もCSV末尾に追加される。 |
| 08 | 管理者機能 | 日付フィルタ | 正常系 | 日付を指定してデータをフィルタリングする | 指定日付のタスクデータのみ表示されること | ○ | `<select>`の日付プルダウンで`selectedDate`を変更すると、useEffectにより`loadDayData(selectedDate)`が実行され、該当日のデータのみが表示される。 |
| 09 | 管理者機能 | 日付フィルタ | 正常系 | 日付範囲を指定してデータをフィルタリングする | 指定期間のタスクデータが表示されること | × | **日付範囲指定機能は未実装。** 現在は単一日付の選択のみ対応。日付プルダウンで1日分のデータを切り替えて参照する仕様。日付範囲指定（From-To）でのフィルタリング機能が必要な場合は追加実装が必要。 |
| 10 | 管理者機能 | 地域フィルタ | 正常系 | 地域を指定してデータをフィルタリングする | 指定地域のタスクデータのみ表示されること | × | **地域フィルタ機能は未実装。** 管理者画面では全7地域の進捗が常に一覧表示される。特定地域のみにフィルタリングする機能はない。CSVダウンロードも全地域分のデータが出力される。 |
| 11 | 管理者機能 | データ参照 | 正常系 | 過去データを参照できる | 2ヶ月以内の過去データが管理画面で参照できること | ○ | `getAvailableDates`でlocalStorageの`task-checker-v3-`キーを走査し、日付リストを生成。日付プルダウンで過去の日付を選択すると`loadDayData`で該当日のデータが読み込まれ表示される。 |
| 12 | 管理者機能 | 画面更新 | 正常系 | 管理画面を手動更新して最新データを取得する | ページ更新後、最新のタスク進捗が反映されること | ○ | ブラウザのページ更新時にコンポーネントが再マウントされ、useEffectで`loadDayData(selectedDate)`が再実行される。作業者画面で保存されたlocalStorageの最新データが反映される。 |
| 13 | 管理者機能 | CSV出力 | 異常系 | データが存在しない状態でCSV出力する | 空のCSVまたは適切なメッセージが表示されること | △ | `downloadCSV`関数先頭で`if (!dayData) return;`のnullチェックはあるが、dayDataが空（completedTasksが0件）の場合でもヘッダ行のみのCSVが出力される。**「データが存在しません」等のメッセージ表示は未実装。** 空のCSVが生成されダウンロードされるが、ユーザーへの明示的な通知はない。 |
| 14 | 管理者機能 | 地域フィルタ | 異常系 | 全地域フィルタで全データが表示される | 全地域のデータが一覧表示されること | - | 地域フィルタ機能自体が未実装（No.10参照）のため、常に全地域データが表示されている。本項目はフィルタ機能の実装後に再検査が必要。 |
| 15 | 管理者機能 | データ参照 | 異常系 | 2ヶ月以上前のデータを参照する | 2ヶ月以上前のデータは表示されないこと（自動削除済み） | ○ | `cleanupOldData`により2ヶ月超過データは起動時に自動削除される。`getAvailableDates`は現存するlocalStorageキーのみを返すため、削除済みの日付はプルダウンに表示されず、参照不可。 |

---

## 検査結果サマリ

| シート | 合格(○) | 不合格(×) | 条件付き(△) | 未実施(-) | 合計 |
|---|---|---|---|---|---|
| (1)作業者ログイン画面 | 7 | 1 | 0 | 0 | 8 |
| (2)管理者ログイン画面 | 6 | 1 | 2 | 0 | 10 (1件は○相当だが権限仕様の明確化が必要) |
| (3)地域選択画面 | 8 | 0 | 0 | 0 | 8 |
| (4)タスク一覧画面 | 12 | 0 | 3 | 0 | 15 |
| (5)業務日報入力画面 | 5 | 0 | 5 | 0 | 11 (△は仕様範囲外の検査項目を含む) |
| (6)管理者ダッシュボード | 9 | 3 | 2 | 1 | 15 |
| **合計** | **47** | **5** | **12** | **1** | **67** |

---

## 不合格(x)項目一覧と対策

| シート | No. | 項目 | 対策案 |
|---|---|---|---|
| (1) | 05 | ログアウトボタン未実装 | 作業者画面ヘッダーにログアウトボタンを追加し、`sessionStorage.removeItem("auth-user")`実行後にログイン画面を再表示する |
| (2) | 05 | 管理者ログアウトボタン未実装 | 管理者画面ヘッダーにログアウトボタンを追加し、`sessionStorage.removeItem("auth-admin")`実行後にログイン画面を再表示する |
| (6) | 03 | 遅延地域の強調表示未実装 | 管理者ダッシュボードで各地域の遅延タスク件数を集計し、遅延がある地域カードにアラートアイコンまたは黄色枠を表示する |
| (6) | 09 | 日付範囲指定機能未実装 | 日付プルダウンをFrom-Toの範囲選択に変更し、期間内の集計データを表示する機能を追加する |
| (6) | 10 | 地域フィルタ機能未実装 | 管理者ダッシュボードに地域フィルタプルダウンを追加し、選択地域のみのデータ表示・CSV出力に対応する |

---

## 条件付き合格(△)項目一覧

| シート | No. | 項目 | 条件・備考 |
|---|---|---|---|
| (2) | 06 | 作業者による管理者画面アクセス | ログイン画面は表示されるが、admin/adminを入力すればアクセス可能。ロールベースのアクセス制御は今後の要件に応じて検討 |
| (2) | 07 | 管理者による作業者画面アクセス | No.06と同様。相互排他的アクセス制御は要件に応じて検討 |
| (4) | 01 | タスク一覧の日付表示 | 地域名は表示されるが当日日付が未表示。軽微な改修で対応可能 |
| (4) | 12 | 未完了タスクのリアルタイム遅延検知 | 完了時の遅延判定は実装済み。未完了のままの場合のリアルタイムアラートは要件に応じて検討 |
| (5) | 04 | 日報への地域自動記録 | 日報は共通データとして管理。地域別日報が必要な場合は仕様検討が必要 |
| (5) | 05 | タスク完了数の日報反映 | プログレスバーで確認可能だが日報内への自動反映は未実装 |
| (5) | 06 | 作業者画面での過去日報参照 | 管理者画面からは参照可能。作業者画面への機能追加は要件に応じて検討 |
| (5) | 07 | 作業者画面での日付指定検索 | 管理者画面では対応済み。作業者画面への機能追加は要件に応じて検討 |
| (5) | 10 | 日報の印刷・出力機能 | CSVに日報データは含まれる。単体の印刷機能は要件に応じて検討 |
| (4)(タスク) | - | - | - |
| (6) | 13 | データ無し時のCSV出力 | 空CSVが出力されるが明示的メッセージは未表示。UX改善として検討 |
